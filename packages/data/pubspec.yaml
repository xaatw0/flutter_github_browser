name: data
description: A starting point for Dart libraries or applications.
version: 1.0.0
publish_to: 'none'

environment:
  sdk: ^3.6.0
resolution: workspace

dependencies:
  domain:
    path: ../domain
  dio: ^5.7.0

dev_dependencies:
  build_runner: ^2.4.14
  lints: ^5.0.0
  mockito: ^5.4.5
  test: ^1.24.0
